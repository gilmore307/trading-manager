# Catalog Helpers

Migrated from the former standalone registry repository.

These helpers are preserved as the starting point for trading registry access. Update them to read from `trading-main/registry/` or the future `trading_registry` database before treating them as an accepted runtime interface.

Until updated, treat them as preserved source material, not an accepted runtime interface.
