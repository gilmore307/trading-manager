# Registry Reviews

This directory holds registry review notes, boundary assessments, and watch-list findings.

It does not own registry kind definitions. Kind source-of-truth files live under `../kinds/`.
