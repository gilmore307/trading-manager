# Ready Signal Type

## Kind Boundary

Registered ready-signal type values used to classify downstream consumability signals.

## Range

Register ready-signal type values only. Do not register readiness statuses, manifest types, request types, or alert payload schemas here.

## Reject Or Re-scope

Reject or re-scope entries that are actually:

- status values;
- manifest types;
- request types;
- alert schemas;
