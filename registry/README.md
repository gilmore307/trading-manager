# Registry

`registry/` is the canonical home for trading-wide registered names.

Use this directory for fields, identifiers, statuses, artifact types, request types, and other shared vocabulary that component repositories must not redefine locally.

Do not store secrets, generated data, or component-local implementation notes here.
