# Shared trading Python environment dependencies.
#
# Runtime anchor: /root/projects/trading-main/.venv
# Python: 3.12
# Installer: python -m pip install -r requirements.txt
#
# Add dependencies only through reviewed changes. Keep this file empty until a
# component has an accepted dependency need.
