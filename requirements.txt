# Shared trading Python environment dependencies.
#
# Runtime anchor: /root/projects/trading-manager/.venv
# Python: 3.12
# Installer: python -m pip install -r requirements.txt
#
# Add dependencies only through reviewed changes.

# Packaging backend for editable installs of trading-manager helper package.
setuptools>=69,<76

# Cryptographic signing support used by the shared BigQuery REST helper.
cryptography>=42,<47

# PostgreSQL driver used by trading component SQL runners through the shared
# trading-manager environment anchor.
psycopg[binary]>=3.1,<4

# JSON Schema validation for manager control-plane contract examples and write-path gates.
jsonschema>=4.10,<5

# ThetaData Python library for direct historical option data acquisition through
# the shared trading-manager environment.
thetadata>=1,<2
